{
  "name": "@ki1r0y/cache",
  "type": "module",
  "exports": {
    "default": "./index.mjs"
  },
  "version": "1.0.1",
  "description": "Simple, fast, least-recently-used cache (specifically, last set) with active time-to-live eviction, optimized for large-string keys and frequent reads, and no dependencies (other than jasmine dev testing).",
  "scripts": {
    "test": "jasmine"
  },
  "keywords": [
    "cache",
    "lru",
    "ttl",
    "map",
    "large-keys"
  ],
  "author": "howard.stearns@gmail.com",
  "license": "MIT",
  "devDependencies": {
    "jasmine": "^5.1.0"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/kilroy-code/distributed-security.git"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org"
  }  
}
